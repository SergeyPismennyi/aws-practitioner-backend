{
  "name": "@aws-practitioner/types",
  "version": "0.0.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "format": "prettier --config .prettierrc.js --write \"**/*.{js,ts}\"",
    "lint": "eslint ./src",
    "lint:fix": "eslint ./src --fix"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
